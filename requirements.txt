# ── Core (stages 00–01) ──────────────────────────────────────────────────────
requests>=2.31
python-dotenv>=1.0
pytesseract>=0.3.10
Pillow>=10.0
pyyaml>=6.0
pandas>=2.0
openpyxl>=3.1
easyocr>=1.7
paddlepaddle-gpu==2.6.2
paddleocr==2.7.3
numpy<2.0                  # paddle ABI requires numpy 1.x

# ── Figure segmentation + OCR (GoFigure-LANL) ────────────────────────────────
tensorflow>=2.10          # HR-Net model (segmenter.py)
opencv-python>=4.8        # mask post-processing
scikit-image>=0.21        # alpha-shape OCR labeler
scipy>=1.11               # Delaunay triangulation for alpha-shape
gdown>=4.7                # download HR-Net weights from Google Drive

# ── Deduplication + grouping (T3 / T0 pipeline stages) ───────────────────────
rapidfuzz>=3.0             # fuzzy company-name matching
sentence-transformers>=2.2 # PatentSBERTa embeddings for prototype clustering

# ── Stage 00a.2 triage (SigLIP zero-shot pre-filter) ─────────────────────────
open-clip-torch>=2.24         # SigLIP ViT-SO400M-14-SigLIP-384 (also used by cross_modal.py)

# ── Stages 02–05 (install when you reach each stage) ─────────────────────────
torch>=2.0
# Pinned below 5.0: transformers>=5 imports torch.float8_e8m0fnu at module
# load time (transformers/integrations/finegrained_fp8.py), which doesn't
# exist before torch 2.7 — breaks loading PatentSBERTa/MPNetModel with a
# misleading "Could not import module 'MPNetModel'" ModuleNotFoundError.
transformers>=4.35,<5.0
scikit-learn>=1.3          # includes HDBSCAN (≥1.3) used for prototype clustering
umap-learn>=0.5

# ── Notebooks ────────────────────────────────────────────────────────────────
jupyter>=1.0
ipywidgets>=8.0
matplotlib>=3.8
